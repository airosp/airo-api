# binários
/bin/
/airo-api
/airo-worker

# ambiente — nunca versionado
.env
.env.*
!.env.example

# ferramentas
.DS_Store
*.test
*.out
coverage.*

# O binário do gerador da especificação, que tem o nome da pasta. A barra
# fixa-o à raiz: sem ela, o `.gitignore` apanhava também cmd/openapi/.
/openapi

